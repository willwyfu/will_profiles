function recursive() {
	cd "$1"
	ISDIR=`ls -l | grep "^d"`
	if [ "$ISDIR" == "" ]; then
		echo `pwd`;
 		$2
		cd ..
		return
	fi
	SAVEIFS=$IFS
	IFS=$(echo -en "\n\b")
	for DIR_NAME in `ls -d */`
	do
	IFS=$SAVEIFS
		recursive "$DIR_NAME" "$2"
	done
	cd ..
}
recursive . "$@"

