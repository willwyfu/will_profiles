#!/bin/sh

oamount
if [ -z $1 ] | [ -z $2 ] ; then
	echo "rocp folder file"
	exit 0
fi

TAGET_BASE=~/OaTemp/temp/Will
FOLDER=$1

shift
TARGET=${TAGET_BASE}/${FOLDER}
mkdir -p ${TARGET}
echo Copy "$@" to ${TARGET}
cp $@ ${TARGET}/
